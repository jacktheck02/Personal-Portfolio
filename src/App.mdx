import { For } from 'solid-js';
import WireframeBall from './components/WireframeBall.tsx';
import ProjectLink from './components/ProjectLink.tsx';
import Circle from './components/Circle.tsx';
import { projects, contactInfo, mappingIcons } from './constants.js';


<div class="flex flex-col-reverse md:flex-row items-center justify-between gap-12">
  <div class="flex-1">
    <p class="text-5xl font-extrabold text-nord-6 mb-4 tracking-tight">Jack Heckenlaible</p>
    <p class="text-xl text-nord-4 leading-relaxed max-w-lg mb-6">
      a software engineer with a passion for building apps that help others, explore football (soccer) data, or both.
    </p>
    <div class="flex gap-2">
    <For each={contactInfo} fallback={<div class="text-nord-4">Loading...</div>}>
      {(item) => <a
          href={item.href}
          target="_blank"
          rel="noopener noreferrer"
        >
          <Circle size={48}>
            {mappingIcons[item.name]({ size: 24 })}
          </Circle>
        </a>
       }
    </For>
    </div>
  </div>
  <Circle size={256}>
    <WireframeBall />
  </Circle>
</div>

## About Me

Having recently graduated with my master's degree, I have explored a lot of different tools, systems, and patterns in a relatively short period of time.  

I have professional experience with web apps, restful APIs, workflow engines, cloud infrastructure, and batch processors. All of these systems I have developed to be built with efficiency and quality in mind.

 > "I've suffered a great many catastrophes in my life. Most of them never happened." - Mark Twain


## Projects

Here are some of the projects I've been building recently.

<ul class="list-disc pl-6 space-y-4">
  <For
    each={projects}
    fallback={<li class="text-nord-4">No projects to show yet...</li>}
  >
    {(project) => (
      <li>
        <strong>{project.name}</strong>: {project.intro}{' '}
        <ProjectLink
          href={project.href}
          title={project.title}
          description={project.description}
          tags={project.tags}
        >
          {project.linkText}
        </ProjectLink>{' '}
        {project.outro}
      </li>
    )}
  </For>
</ul>

